import 'package:flutter/material.dart';

class TIconColorTheme {
  TIconColorTheme._(); // Private constructor to prevent instantiation

  static const Color iconColorLight = Colors.black; // Define the icon color for light theme
  static const Color iconColorDark = Colors.white; // Define the icon color for dark theme
}
