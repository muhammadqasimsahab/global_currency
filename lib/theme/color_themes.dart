import 'package:flutter/material.dart';

class TColorTheme {
  TColorTheme._();

  // Light mode colors
  static const Color primaryColorLight = Color(0xFF6200EE);
  static const Color accentColorLight = Color(0xFF609FFF);
  static const Color backgroundColorLight = Color(0xFFEFEFEF);
  static const Color textColorLight = Colors.black;
  static const Color dividerColorLight = Colors.grey;

  // Dark mode colors
  static const Color primaryColorDark = Color(0xFFBB86FC);
  static const Color accentColorDark = Color(0xFF609FFF);
  static const Color backgroundColorDark = Color(0xFF252525);
  static const Color textColorDark = Colors.white;
  static const Color dividerColorDark = Color(0xFF424242);

  // Icon color for dark mode
  static const Color iconColorDark = Colors.white;
}
